#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int t;
    cin >> t;
    while (t--)
    {
        long long int n, m, i, j;
        cin >> n >> m >> i >> j;
        cout << 1 << " " << 1 << " " << n << " " << m << " " << endl;
    }
    return 0;
}