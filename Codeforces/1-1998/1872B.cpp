#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int d[n], s[n];
        for (int i = 0; i < n; i++)
        {
            cin >> d[i] >> s[i];
        }
    }
    return 0;
}