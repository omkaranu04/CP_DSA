#include <bits/stdc++.h>
using namespace std;
#define ll long long int
ll ans(ll n, ll k)
{
    if(n==1) return 1;
}
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll q;
    cin >> q;
    while (q--)
    {
        ll n, k;
        cin >> n >> k;
    }
    return 0;
}