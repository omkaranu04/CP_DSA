#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define endl "\n"
const int mod = 1e9 + 7;
ll dp[100100][2][2];
ll len;
ll rec(ll level, ll lth, ll rth)
{
    if(level == len) return (lth == 0 && rth == 0)
}
void solve()
{
    string P;
    cin >> P;
    len = P.length();
}
int main(int argc, char const *argv[])
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}